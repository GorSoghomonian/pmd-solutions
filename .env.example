# API Base URLs
# Server-side API URL (for server components and API routes)
HUBSPOT_BASE_URL=https://readdy.ai

# Client-side API URL (must start with NEXT_PUBLIC_ to be exposed to browser)
NEXT_PUBLIC_API_URL=https://readdy.ai
